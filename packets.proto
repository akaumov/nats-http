syntax = "proto3";
package pb;

message Request {
    string method = 1;
    string host = 2;
    string remoteAddr = 3;
    string requestURI = 4;
    bytes body = 5;
}

message Response {
    int64 status = 1;
    bytes body = 2;
}